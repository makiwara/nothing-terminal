# Minification is disabled (build.gradle.kts: isMinifyEnabled = false), so this is
# a placeholder. If you enable R8, keep the Termux terminal classes accessed via
# the AAR:
# -keep class com.termux.terminal.** { *; }
# -keep class com.termux.view.** { *; }
